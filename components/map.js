import React, {useEffect, useRef} from "react";

function Int

const Map = ({location}) => {
    let map = useRef(null);
    useEffect(() => {

    }, [])
    return (
        <StyledMap ref={map}></StyledMap>
    )
}